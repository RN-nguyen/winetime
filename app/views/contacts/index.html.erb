<h1>Contact List</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Subject</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.name %></td>
    <td><%= contact.email %></td>
    <td><%= contact.subject %></td>
    <td><%= contact.body %></td>
    <td><%= link_to 'Contact Me', new_contact_path %></td>
    <!-- <td><%= link_to 'Edit', edit_contact_path(contact) %></td> -->
    <td><%= link_to 'Destroy', contact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Contact Me', new_contact_path %>
